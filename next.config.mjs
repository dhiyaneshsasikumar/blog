/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['contenthub-static.grammarly.com','firebasestorage.googleapis.com'], // Add the domain here
      },
};



export default nextConfig;
